[package]
name = "dolphin-milk-sdk"
version = "0.1.0"
edition = "2021"
description = "SDK for building Dolphin Milk plugins (tools, skills, providers)"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
async-trait = "0.1"
thiserror = "2"
